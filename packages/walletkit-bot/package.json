{
  "private": false,
  "name": "@waveshq/walletkit-bot",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@stickyjs/jest": "^1.3.1"
  },
  "scripts": {
    "build": "tsc -b ./tsconfig.build.json",
    "test": "jest --maxWorkers=100%",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "jest": {
    "preset": "@stickyjs/jest"
  },
  "dependencies": {
    "@waveshq/walletkit-core": "workspace:*",
    "bignumber.js": "^9.1.1"
  }
}
