{
  "private": false,
  "name": "@waveshq/walletkit-testcontainers",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@birthdayresearch/sticky-jest": "^0.5.7"
  },
  "scripts": {
    "build": "tsc -b ./tsconfig.build.json",
    "test": "jest --maxWorkers=100%",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "jest": {
    "preset": "@birthdayresearch/sticky-jest"
  },
  "dependencies": {
    "bignumber.js": "^9.1.1"
  }
}
