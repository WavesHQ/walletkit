{
  "private": false,
  "name": "@waveshq/walletkit-core",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b ./tsconfig.build.json",
    "test": "jest --maxWorkers=100%",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "jest": {
    "preset": "@birthdayresearch/sticky-jest"
  },
  "dependencies": {
    "bignumber.js": "^9.1.1",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@types/react": "18.2.0",
    "@birthdayresearch/sticky-jest": "^0.6.2"
  }
}
