{
  "private": false,
  "name": "@waveshq/walletkit-core",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@birthdayresearch/sticky-jest": "^0.4.1"
  },
  "scripts": {
    "build": "tsc -b ./tsconfig.build.json",
    "test": "jest --maxWorkers=100%",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "jest": {
    "preset": "@birthdayresearch/sticky-jest"
  },
  "dependencies": {
    "bignumber.js": "^9.1.1",
    "ethers": "^6.3.0"
  }
}
